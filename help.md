# <><><><><><><><> #
1. requirements.txt. ---- It contains the exact library versions which our bot needs 
   Use: 
   a. pip install -r requirements.txt => it will install python-telegram-bot {Telegram API wrapper + job queue for the tick}
   b. python-dotenv (reads .env)

2. .env.example ---- template of the setting which bot reads at the very starting ! 